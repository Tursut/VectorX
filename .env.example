# Copy to `.env.local` for per-developer overrides (gitignored via *.local).
# Repo-wide defaults live in `.env`.

# Toggle online multiplayer UI (Create Room / Join Room). Server + client
# plumbing ship gradually; UI stays hidden until Step 16 lands.
VITE_ENABLE_ONLINE=false
