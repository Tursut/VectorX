// Online multiplayer shell. Stubbed for Step 3 — the real implementation
// arrives in Steps 13–16 (network client, useNetworkGame hook, Lobby/Join UI).
// Until `VITE_ENABLE_ONLINE=true`, App's router never reaches this branch and
// Vite tree-shakes this module out of the production bundle.

export default function OnlineGameController() {
  return null;
}
